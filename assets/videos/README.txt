Drop the 5 hero videos here using these exact filenames (or update the
<source src="..."> paths in index.html if you name them differently):

  01-meridian-cultural-center.mp4
  02-lattice-tower.mp4
  03-riverline-residences.mp4
  04-horizon-transit-hub.mp4
  05-the-courtyard-house.mp4

Tips:
- Keep each clip short and loop-friendly (10-20s), muted, no dialogue needed
  since the <video> tags are set to muted + loop + autoplay.
- Compress to roughly 1920x1080, h.264 mp4, under ~8-10MB each if possible,
  so the hero loads fast.
- Once a file is in place, uncomment the matching <source> line in
  index.html and remove (or leave, it'll just sit unused behind the video)
  the .hero-placeholder div for that slide.
